%h1 Listagem de Avaliações

%table
  %thead
    %tr
      %th Peso
      %th Altura
      %th Percentual de Gordura
      %th Imc
      %th Cliente
      %th
      %th
      %th

  %tbody
    - @avaliacoes.each do |avaliacao|
      %tr
        %td= avaliacao.peso
        %td= avaliacao.altura
        %td= avaliacao.percentualGordura
        %td= avaliacao.imc
        %td= avaliacao.cliente.nome
        %td= link_to 'Visualizar', avaliacao
        %td= link_to 'Editar', edit_avaliacao_path(avaliacao)
        %td= link_to 'Excluir', avaliacao, :method => :delete, :data => { :confirm => 'Você tem certeza?' }

%br

= link_to 'Cadastrar Avaliação', new_avaliacao_path
